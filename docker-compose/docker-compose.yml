services:
  api:
    build:
      context: ..
      dockerfile: docker-compose/Dockerfile
    container_name: notarymindai-api
    working_dir: /workspace
    environment:
      PORT: "8787"
      FLASK_ENV: production
      PYTHONUNBUFFERED: "1"
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      GITHUB_COPILOT_API_BASE: ${GITHUB_COPILOT_API_BASE:-https://api.githubcopilot.com}
      OPENAI_API_BASE: ${OPENAI_API_BASE:-https://api.openai.com}
      ANTHROPIC_API_BASE: ${ANTHROPIC_API_BASE:-https://api.anthropic.com}
      GENAI_PROVIDER: ${GENAI_PROVIDER:-copilot}
      NOTARYMIND_OCR_MODEL: ${NOTARYMIND_OCR_MODEL:-github-copilot/claude-opus-4.8}
      NOTARYMIND_INTERPRET_MODEL: ${NOTARYMIND_INTERPRET_MODEL:-github-copilot/haiku-4.5}
      NOTARYMIND_MAP_MODEL: ${NOTARYMIND_MAP_MODEL:-github-copilot/haiku-4.5}
      GENAI_MAX_TOKENS: "12000"
    ports:
      - "8787:8787"
    volumes:
      - ..:/workspace:rw
    command: python3 api/server.py
    restart: unless-stopped
